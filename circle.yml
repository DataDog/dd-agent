machine:
  python:
    version: 2.7.9
  environment:
    SKIP_CLEANUP: true
    # comma separated list
    CIRCLECI_FLAVORS: docker

dependencies:
  pre:
    - pip install --use-wheel pyro4==4.35
    - bundle install --system

test:
  override:
    - ./tests/circleci.sh:
        parallel: true
