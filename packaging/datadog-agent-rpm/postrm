case "$*" in
  0) 
    # We're uninstalling.
    getent passwd datadog > /dev/null && userdel datadog
    getent group datadog >/dev/null && groupdel datadog
    ;;
  1)
    # We're upgrading. 
    ;;
  *) 
    ;;
esac

exit 0
