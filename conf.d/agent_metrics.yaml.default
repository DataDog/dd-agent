init_config:
    process_metrics:
        get_memory_info: yes
        get_io_counters: yes
        get_num_threads: yes
        get_connections: no
    general:
        foo: bar

instances:
  # No configuration is needed for this check.
  # A single instance needs to be defined with any value.
  - foo: bar
